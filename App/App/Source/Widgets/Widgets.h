#include "../Global.h"

//
namespace Widgets {
	namespace Nav {
		namespace Buttons {
			void Main(float buttonWidth);
			void Side(float buttonWidth);
			void Login(float buttonWidth);
		}
		namespace Menu {
			void Main();
			void Side();
			void Login();
		}
	}
	namespace Deco
	{
		void Footer();
	}
	
} // namespace Widgets